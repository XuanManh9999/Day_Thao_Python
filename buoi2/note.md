# Để dùng được cái gì thì ta cần phải import(lấy) lấy nó

# Hàm builin và hàm trong thư viện toán học math

# Hàm builin là hàm mà đã được xây dựng sẵn, ta không cần phải import thư viện (mặc định trong ngôn ngữ lập trình đã có rồi)

# Hàm math thì ta cần import để dùng

